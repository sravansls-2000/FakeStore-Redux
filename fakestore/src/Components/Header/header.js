import React from 'react';
import { LogoName } from '../../constants/utils';
import './header.css';

const Header = () => {
  return <div className="header">{LogoName}</div>;
};

export default Header;
